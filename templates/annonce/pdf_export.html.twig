<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des annonces</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            text-align: left;
            padding: 8px;
        }

        th {
            background-color: #ddd;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
<h1>Liste des annonces</h1>
<table>
    <thead>
    <tr>
        <th>Type</th>
        <th>Description</th>
        <th>Image</th>
        <th>Date de l'annonce</th>
    </tr>
    </thead>
    <tbody>
    {% for annonce in annonces %}
        <tr>
            <td>{{ annonce.type }}</td>
            <td>{{ annonce.description }}</td>
            <td><img src="{{ asset('uploads/images/' ~ annonce.image) }}" alt="{{ annonce.image }}"></td>
         
            <td>{{ annonce.dateAnnonce|date('d/m/Y') }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>
